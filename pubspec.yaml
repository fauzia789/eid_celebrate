name: eid_celebrate
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.36  
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2 
  flutter_local_notifications: ^17.0.0+1 
  timezone: ^0.9.2 
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audio/
